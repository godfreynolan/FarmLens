# platform :ios, '9.0'
use_frameworks!

target 'FarmLens' do
   pod 'DJI-SDK-iOS', '~> 4.4'
   pod 'Mapbox-iOS-SDK', '~> 3.7'
   pod 'Fabric'
   pod 'Crashlytics'
   
   target 'FarmLensTests' do
       inherit! :search_paths
   end
end
